name: oracle
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - langgraph
      - langchain-ollama
      - langchain-core
      - langchain-anthropic
      - pyyaml
      - pydantic-settings
      - faster-whisper
      - kokoro-onnx
      - sounddevice
      - pyaudio
      - openwakeword
      - scipy
      - rich
      - httpx
      - pytest
